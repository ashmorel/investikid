target-version = "py312"
line-length = 120

exclude = [
    "alembic/versions/*.py",
]

[lint]
select = ["E", "F", "I", "UP"]

[lint.per-file-ignores]
"app/seed/*.py" = ["E501"]
# Test data/setup lines are often intentionally long for readability.
"tests/*.py" = ["E501"]
