# Invest-Ed Security Audit — 2026-05

## Scope
Backend (FastAPI) + frontend (React/TS) app code + dependency CVEs. LLM surface: audit/document only (fixes deferred to AI sub-project). Excludes infra/DAST/pentest.

## Threat Model
### Assets
Child PII (email, dob, username, parent_email, progress); parent PII + parent-session tokens; JWT access/refresh tokens; CSRF tokens; one-time tokens (consent/verify/reset); simulator virtual state; audit logs; LLM provider API keys.
### Trust boundaries
unauth <-> authed child; child <-> parent; app <-> LLM providers; app <-> email provider.
### Actors
unauth attacker; malicious authed child; malicious authed parent (cross-family IDOR); compromised dependency; network MITM.
### Highest-impact (prioritise)
A01 access control / IDOR on /parent/*, /users/*, simulator, content; A02 secret/cookie/transport handling; A07 auth/session integrity; A09 PII/secret leakage in logs/errors.

## Severity Rubric
- **Critical**: unauth remote compromise OR mass child-PII exposure.
- **High**: authed privilege escalation / cross-family IDOR / single-user PII exposure / auth bypass.
- **Medium**: security misconfig with limited blast radius; info leak requiring chained conditions.
- **Low**: defence-in-depth gaps; hardening nits.

## Findings Register
| ID | OWASP | Title | Severity | Location | Status | Fix commit | Regression test |
|----|-------|-------|----------|----------|--------|------------|-----------------|
| DEP-001 | A06 | python-jose: algorithm confusion with OpenSSH ECDSA keys (PYSEC-2024-232) | High | requirements.txt: python-jose==3.3.0 | Fixed | see commit | test_jose_version_not_vulnerable |
| DEP-002 | A06 | python-jose: JWT bomb via crafted JWE token (PYSEC-2024-233) | High | requirements.txt: python-jose==3.3.0 | Fixed | see commit | test_jose_version_not_vulnerable |
| DEP-003 | A06 | pytest: local privilege escalation via /tmp dir (GHSA-6w46-j5rx-g56g) | Low | requirements.txt: pytest==8.3.2 | Open | | |
| DEP-004 | A06 | starlette: main thread block on large multi-part file upload (GHSA-2c2j-9gv5-cj73) | Medium | requirements.txt: starlette==0.41.3 | Open | | |
| DEP-005 | A06 | starlette: quadratic-time Range header parsing DoS (GHSA-7f5h-v6xp-fcq8) | Medium | requirements.txt: starlette==0.41.3 | Open | | |
| NPM-001 | A06 | esbuild: dev server allows cross-origin requests (GHSA-67mh-4wv8-2f99) | Medium | package-lock.json: esbuild==0.21.5 | Open | | |
| NPM-002 | A06 | vite: path traversal in optimised deps .map handling (GHSA-4w7w-66w2-5vf9) | Medium | package-lock.json: vite==5.4.21 | Open | | |
| A08-PIN | A08 | Unpinned backend dependencies | Low | requirements.txt | Fixed | see commit | test_requirements_fully_pinned |
| A01-1 | A01 | SentEmail cross-child export IDOR | High | `app/services/export_service.py` + `app/models/consent.py` | Fixed | see commit | `test_export_does_not_leak_sibling_emails` |
| A05-1 | A05 | Authenticated state-changing endpoint `POST /tutor/chat` is CSRF-exempt — bypasses double-submit CSRF defence for a session-authed mutating route (persists conversation, consumes child's tutor quota, drives billed LLM calls). Compensating control before fix: SameSite=Lax on `access_token` | Medium | `app/core/csrf.py:31` (`_DEFAULT_EXEMPT_PATHS` contained `/tutor/chat`) | Fixed | see commit | `test_csrf_tutor.py::test_tutor_chat_rejects_request_without_csrf_header` |
| A07-1 | A07 | `decode_token` accepts any non-`refresh` JWT as a session — a claims-only (typeless, audless) JWT signed with the app secret authenticates `/users/me` etc.; only positive `type=="access"` check was missing (one-time/parent-session vectors are transitively blocked by jose 3.5.0's aud-presence check, asserted in the regression test as the boundary) | High | `app/core/security.py:46` (`decode_token`) + `app/routers/auth.py:56` (`_set_access_cookie`) | Fixed | see commit | `test_token_audience_confusion.py` (`test_typeless_audless_jwt_rejected_as_access_cookie` + 2 boundary asserts), `test_security.py::test_typeless_token_rejected_as_access` |

## Coverage Log
(One row per audited area: "AREA - reviewed - finding IDs or NO FINDING")
| Area | Status |
|------|--------|
| bandit (backend app/ -ll Medium+) | reviewed - NO FINDING |
| pip-audit (backend requirements.txt) | reviewed - DEP-001, DEP-002, DEP-003, DEP-004, DEP-005 |
| npm audit (frontend) | reviewed - NPM-001, NPM-002 |
| SCANNERS | reviewed - DEP-001, DEP-002, DEP-003, DEP-004, DEP-005, NPM-001, NPM-002 |
| Dependency pinning (A08) | reviewed - A08-PIN |
| A01 SentEmail export scoping | reviewed - A01-1 |
| A01 Broken Access Control / IDOR (parent.py freeze/erasure/export, users.py /me*, consent.py, auth.py token endpoints, simulator.py portfolio/holdings/trades, content.py, gamification.py, ai.py) | reviewed - NO FINDING (every parent `{user_id}` route guarded by `_get_owned_child` parent_email scope w/ existing cross-parent 404 tests; users/simulator/content/gamification/ai all `current_user.id`-scoped, no client-supplied id; consent/auth token routes scoped by signed one-time tokens) |
| A02 Cryptographic Failures (security.py hashing/cookies, auth.py cookie issuance, JWT alg pinning x3 jose decode sites, tokens.py entropy/expiry) | reviewed - NO FINDING (Argon2; all cookies httponly+samesite=lax, secure when env!=development; algorithms=[...] pinned at security.py:48, tokens.py:71, tokens.py:113, consent.py:39; uuid4 jti + DB single-use + expiry) |
| A03 Injection (grep raw SQL / shell / eval / unsafe-deserialisation + raw SQL audit) | reviewed - NO FINDING (all DB access SQLAlchemy Core/ORM parameterised; no raw/interpolated SQL, shell, eval, or unsafe deserialisation) |
| A04/A05 Insecure Design / Misconfiguration (main.py CORS+middleware order, exception leak, settings debug, security headers, slowapi coverage, seed prod guard) | reviewed - A05-1 (also: CORS not `*`+credentials; unhandled 500 returns generic `Internal Server Error` no traceback; CSP/HSTS present; auth/sensitive endpoints rate-limited or one-time-token gated; `seed_compliance_accounts` early-returns on production) |
| A06 (app-level constructs only; deps owned by tasks 3/4/8) | reviewed - NO FINDING (no app-code dangerous construct beyond DEP-001..005/NPM-001..002/A08-PIN already registered) |
| A07 Identification & Auth Failures (token type/aud substitution, lockout bypass/enumeration, one-time token replay) | reviewed - A07-1 (lockout: identical 401 "Invalid credentials" for unknown/wrong-pw/locked + dummy_verify timing; one-time tokens single-use atomic consume + expiry → replay 410) |
| A08 Software/Data Integrity (untrusted deserialisation, one-time-token tamper resistance) | reviewed - NO FINDING (no unsafe deserialisation primitives; one-time-token JWT signature+aud verified before DB row trusted; beyond A08-PIN already registered) |
| A09 Logging & Monitoring (grep logging/logger/print + AuditLog/SentEmail PII review) | reviewed - NO FINDING (logs limited to tickers/provider-index/error class; no password/hash/JWT/refresh/CSRF/one-time-token/PII logged; AuditLog stores only user_id/event_type/ip (an unused `metadata_json` column exists on the model but is not written by any current call site); SentEmail.body stores one-time-token links by design — outbox/audit record, recipient receives the token by definition, and cross-principal exposure was remediated under A01-1; error responses generic) |
| A10 SSRF (server-side outbound HTTP with user-influenced URL) | reviewed - NO FINDING (Resend SDK fixed endpoint; LLM base URLs config-only; email links from settings.app_base_url config; yfinance by validated ticker symbol — no user-controlled outbound URL) |

## LLM Surface (Audit-only - Deferred to AI sub-project)
| ID | Risk | Severity | Recommended mitigation | Status |
|----|------|----------|------------------------|--------|

## Dependency Remediation Log
| Package | CVE(s) | Before | After | Status |
|---------|--------|--------|-------|--------|
| python-jose | PYSEC-2024-232, PYSEC-2024-233 | 3.3.0 | 3.5.0 | Fixed |
| pytest | GHSA-6w46-j5rx-g56g | 8.3.2 | | Open |
| starlette | GHSA-2c2j-9gv5-cj73, GHSA-7f5h-v6xp-fcq8 | 0.41.3 | | Open |
| esbuild | GHSA-67mh-4wv8-2f99 | 0.21.5 | | Open |
| vite | GHSA-4w7w-66w2-5vf9 | 5.4.21 | | Open |
