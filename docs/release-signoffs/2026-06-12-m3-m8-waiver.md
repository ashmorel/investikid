# Release waiver — 2026-06-12 promotion (M3–M8 batch)

**Decision (release owner, 2026-06-12):** promote `testing → staging → main` ahead of the
device-QA checklist; prod DB snapshot explicitly declined (prod holds test data; the
migration chain was validated by a full replay on a scratch Postgres, including the
cosmetics ALTER fix `d89a8c6`).

**Batch covered (market-leader roadmap M3–M8):**
- M3 hero-first Home redesign (modules grid removed; tier-driven hero/greeting)
- M4 first-party product analytics (migration `a8b9c0d1e2f3`; ingest; admin dashboard;
  retention cron step)
- M5 pricing & packaging (plan catalog, plan-aware checkout, plan picker — store/Stripe
  product setup deliberately deferred to post-beta)
- M6 outcome-led paywall + parent Mastery Report + digest copy variants
- M7 daily goal + server-push foundation (migration `b9c0d1e2f3a4`; push dormant until
  Firebase/Xcode operator steps; streak-risk cron step)
- M8 Penny cosmetics shop (migration `c0d1e2f3a4b5` incl. retroactive coin backfill)
- Dependabot fixes (9 alerts, dev-scope npm) + assorted m-series fixes

**Conditions (carried forward, now firmer):**
- The full checklist (`docs/release-qa-checklist.md`) runs on a real iPhone (and Android
  when available) **before any external testers are invited** — and now requires the
  Xcode rebuild anyway (new `@capacitor/push-notifications` plugin + M3–M8 UI).
- That run must also cover: new Home hierarchy, daily-goal bar + picker, Penny's Shop
  purchase/equip, plan picker, parent Mastery Report, account deletion.
