# Release waiver — 2026-06-13 promotion (M9)

**Decision (release owner, 2026-06-13):** promote `testing → staging → main` ahead of the
device-QA checklist; prod DB snapshot explicitly declined (migration `d1e2f3a4b5c6` is
purely additive — one defaulted column `challenges.scope` + one empty table
`group_challenge_completions`, no row rewrites; validated by full-chain scratch-Postgres
replay before push).

**Batch covered (market-leader roadmap M9 — final build workstream):**
- Co-op group challenges: `challenges.scope`, group-sum settle with once-per-member
  rewards, `GET /groups/challenges`, Stats group-goal bars, admin scope field.
- Seasonal events: AppSetting-driven (`seasonal_event`), child Home banner, lesson-XP
  bonus (half-up), authored from /admin Settings with no deploy.

**Conditions (carried forward, unchanged):**
- The full checklist (`docs/release-qa-checklist.md`) runs on a real iPhone (and Android
  when available) **before any external testers are invited**, on the next TestFlight
  build (build 4 — includes M3–M9 UI + the `@capacitor/push-notifications` plugin).
- That run should also exercise: group-goal display on Stats, and (admin-side) authoring
  a seasonal event + confirming the Home banner + XP bonus appear for a child.
