# InvestiKid — Master Backlog

**Updated:** 2026-06-25 (native builds uploaded — iOS 16 → TestFlight, Android v3 → Play Internal; Figma-first UI-review programme complete across all surfaces incl. Penny's Shop hero; MoneyWord daily-reset fix. Earlier base, 2026-06-22: tiered lesson depth + module archive/purge + server-side content pipeline; **GB/US/HK content LIVE**; markets tab; LLM credit-fallback; CI speed-up + beta straight-to-main) · **Launch-polish pass (pm):** Learn-tab market switcher, `has_content` deploy-reset fix, route-level ErrorBoundary, scroll-to-top on nav, unified "Back to App", Gemini quota/thinking + provider-agnostic alert. **iOS build 1.0 (16) → TestFlight and Android versionCode 3 → Play Internal testing are UPLOADED (2026-06-25)** — the first Android release; the native bundle carries the full Figma-first UI-review programme + Penny's Shop + the MoneyWord daily-reset fix (native API base = `api.investikid.ai`). See [`docs/launch/2026-06-22-pre-testflight-readiness-checklist.md`](launch/2026-06-22-pre-testflight-readiness-checklist.md).
**Purpose:** the single go-forward list of what's left. Strategic context + per-M
detail live in [`docs/2026-06-12-market-leader-roadmap.md`](2026-06-12-market-leader-roadmap.md);
this is the actionable tracker. Update it as items ship — **and update it (plus any
relevant `docs/superpowers/PROGRESS.md` / roadmap entry) immediately after every push
to `main`, as part of shipping.**

**Owner legend:** 👤 you (human) · ⚙️ operator (console/config, no code) ·
💻 code (dev) · 🤔 decision.

---

## ✅ Live in prod (for context — not outstanding)

- **Frontend CI red→green — `SubscriptionCard` test regression fixed — ✅ 2026-06-25** (main `fa011ab`, **CI fully green, verified** run `28145911777`: Frontend·Security·A11y·Responsive pass, Backend correctly skipped). **Honest correction:** the Frontend CI job (lint·typecheck·**test**·build) had been **failing since `72c39ab` (Parent Phase-2)** and stayed red on every commit after — earlier "CI green" claims this session were unverified (a `gh run watch … | tail; echo EXIT:$?` pattern read **tail's** exit code, not the run's). Deploys were never blocked (Vercel is manual; Railway gates on the **backend** job only), which is why it slipped through. **Root cause:** Parent Phase-2 split the active-subscription copy into a translated title + a hardcoded-English **detail line** + a status badge, but `tests/unit/SubscriptionCard.test.tsx` still asserted the old contiguous strings (`/premium trial/`, `/premium — renews/`, `/premium — cancels/`) — 3 tests, the **only** CI failures (`1 failed | 285 passed`). Fix re-points the assertions at the stable, i18n-independent detail line (`Trial — N days remaining` / `Renews <date>` / `Cancels <date>`); 6/6 pass locally, whole job now green. **Process lesson:** verify CI via `gh run view --json conclusion,jobs`, never `| tail`. Frontend-only test change, no migration/redeploy.

- **Repo professionalization — GitHub config + CI/CD + docs hardening — ✅ 2026-06-25** (commits `abb2692` files, `bdd77d8` workflows; CI green). Audit-driven pass on the public repo. **Public-repo files added:** `README.md` (overview + quick-start + CI/license/platform badges), `LICENSE` (proprietary / all-rights-reserved, source-available), `SECURITY.md` (private disclosure → privacy@invest-ed.app), `CONTRIBUTING.md`, `.github/dependabot.yml` (pip + npm + github-actions, weekly, grouped), `.github/CODEOWNERS`, `.github/pull_request_template.md`, `docs/README.md` index; refreshed the stale `frontend/README.md`. **CI/CD hardening (all 6 workflows):** least-privilege `permissions: contents: read`; `timeout-minutes` on every job (45m cap on the macOS iOS checkpoint job, billed 10×); a **single-Alembic-head CI check** (`alembic heads` == 1) so divergent heads fail PRs not deploys; dropped the no-op `!docs/**` path filter; added the missing CRON_SECRET guard + timeout to the one-off extract cron. **GitHub settings:** description + homepage (`app.investikid.ai`) + 10 topics; **Dependabot security updates + vulnerability alerts enabled**; **`main` ruleset** "main protection" (active — blocks force-push + branch deletion); deleted 3 stale `Invest-Ed /*` environments; delete-branch-on-merge ON; wiki disabled. Already-good and kept: secret-scanning **push protection** + the read-only default workflow token. **Deferred (need the owner / a follow-up):** (1) **CodeQL default setup** — enable via Settings → Code security (the CLI token 404'd on the API); (2) **required-status-checks** in the ruleset need an always-run "CI passed" aggregator job first — the 5 CI jobs are path-conditional, so requiring them directly would deadlock PRs that don't touch every area; (3) scoping `CRON_SECRET` to the production environment needs the secret value (not handled by an agent). Full audit (config / CI / docs) was run via three parallel reviews this session.

- **Native builds uploaded — iOS 1.0 (16) → TestFlight + Android versionCode 3 → Play Internal testing — ✅ 2026-06-25** (version bumps `81dc862`). Web rebuilt + `cap sync ios` / `cap sync android`, build numbers bumped (iOS `CURRENT_PROJECT_VERSION` 15→16, Android `versionCode` 2→3), and a **signed Android release AAB** built locally (`./gradlew :app:bundleRelease`, signed with the upload key from `keystore.properties`, `jar verified`). The user archived iOS in Xcode (automatic signing, team `U2F5E8277U`) and uploaded to **TestFlight**; uploaded the AAB to **Play Internal testing** (Play App Signing enrolled — **first Android release**). Both builds bundle the entire web programme (UI-review redesigns, Penny's Shop + hero, MoneyWord daily-reset fix) **and** the long-held native work (BiometricVault Face ID iOS+Android, push). **Remaining human/operator:** add the processed TestFlight build to a group + assign testers; recruit/distribute the beta cohort; and complete the **kids-app store declarations** (Families / target-audience, Data safety, content rating, privacy-policy URL) that gate wider Play tracks + production + App Store review.

- **MoneyWord daily-reset fix (Home card stuck on "Done") — ✅ shipped to prod 2026-06-25** (main `6efd476`, CI green, **frontend-only, no migration**; web aliased to `frontend-jyqrupdia`; `cap sync ios`). The daily MoneyWord resets at **00:00 UTC** (backend keys `ArcadeDailyPlay` rows on `datetime.now(UTC).date()`; a new UTC day returns a fresh board — server was always correct). The bug was client cache: `useMoneyWordToday` used a **static** key `['arcade','moneyword','today']` (no date), so with `gcTime:24h` + no `staleTime` React Query kept serving yesterday's `completed:true` under the same key after midnight — the cached Home `ArcadeDailyCard` label showed "Done" into the next day (the game page fetches live on mount, so the puzzle itself had reset; only the label was stale). Fix: encode the UTC day in the key (`[...,'today', <YYYY-MM-DD>]`) so the rollover changes the key → fresh refetch. Regression test asserts the key carries today's UTC date. tsc/lint clean; new api test + 6 MoneyWord + 2 Arcade tests pass. **Gotcha for future daily features:** any *cached* `useQuery` for daily/periodic-reset content must encode the period in its key.

- **Figma UI-review programme — Penny's Shop hero redesign — ✅ shipped 2026-06-25** (main `d443675`, CI green `28136163443`, **frontend-only, no migration**; web aliased to `frontend-o3sh2twrk`; `cap sync ios`). Figma-approved structural upgrade (file `uepBJCXaMXmPVtybVMLzgN`) on top of the token sweep: **(1) showcase avatar hero** — `AvatarStage` gains a `hero` variant (full-width, taller, Penny 168px); the shop opens on it with a "Wearing: <accessories>" caption pill (or a "dress Penny up" hint when nothing equipped). **(2) Store-style item tiles** — each item on a tinted disc + name + coin-cost pill (buyable) or "Owned", with the equipped tile getting a brand-600 border + "Wearing" badge; button-state logic/labels unchanged. **(3) Rarity-forward limited drops** — `ActiveDrop` cards take a rarity-coloured border accent (new `rarityBorder` in collectableBits, same conventional ramp as the chip) + emoji-in-disc. Frontend-only, reuses AvatarStage/Penny/cosmetics. tsc clean, 0 lint errors; 29 shop/avatar/collectables/featured tests pass; build clean.

- **Figma UI-review programme — Penny's Shop consistency sweep — ✅ shipped 2026-06-25** (main `16931c8`, CI green `28135267384`, **frontend-only, no migration**; web aliased to `frontend-fiqawdt4f`; `cap sync ios`). Added `/shop` to the programme. Like Learn, the surface was already well-structured (~65–80% consistent) — gaps were purely mechanical token drift, so a baseline sweep (no Figma): `Shop.tsx` hardcoded grays → `text-ink`/`text-muted-foreground`, disabled item-button `bg-gray-100/text-gray-400` → `bg-muted/text-muted-foreground`, off-token `text-red-700` → `text-danger-700`, coin chip padding normalised to `px-2.5 py-1`; `LimitedShelf.tsx` grays → tokens + "Limited edition"/"Owned" headers adopt the shared zone-header pattern; `collectableBits.tsx` progress-bar track + `common` rarity chip → `bg-muted` (legendary/epic/rare amber/purple/sky ramp left intact — conventional rarity palette already shipped on Home's FeaturedDropCard). Layout/behaviour unchanged. tsc/lint clean; 33 shop/collectables/featured/avatar tests pass; build clean. **No structural redesign warranted (no IA problem); offered an optional Figma shop-hero visual pass if wanted.**

- **Figma UI-review programme — Admin form section system (final piece) — ✅ shipped 2026-06-25** (main `eddee6d`, CI green `28134717477`, **frontend-only, no migration**; web aliased to `frontend-elja1neu8`; `cap sync ios`). **This COMPLETES the Figma-first UI-review programme across all surfaces** (Home → Arcade → Admin → Parent, + Learn). Figma-approved (file `qQ6FBTK4E9mWMAspEPAEVB`): a reusable `FormSection` primitive (titled card + optional helper + divider) groups the wall-of-inputs admin forms into scannable sections — **ModuleForm** → Basics / Availability / Rewards / Standards & sources / Lessons (every field id/label/aria-label/handler unchanged, 8 tests green); **ChallengeForm** → Details / Goal & reward / Schedule & access; **AdminSettings** already grouped via semantic `<fieldset>`/`<legend>` (kept for a11y), restyled its section cards to the same `rounded-2xl` + heavier-legend treatment so it reads as one system. Layout-only, no logic/API. tsc/lint clean; full admin suite (129 tests) green; build clean. **No UI-review work outstanding.**

- **Figma UI-review programme — Parent dashboard Phase-2 redesign — ✅ shipped 2026-06-24** (main `72c39ab`, CI green `28111569025`, **frontend-only, no migration**; web aliased to `frontend-hebp1vbd7`; `cap sync ios`). Figma-approved structural redesign of the parent surface (file `6iw0Dddp9W1Fud8GLXwB8I`), the trust/revenue surface. Three changes: **(1) Information architecture** — the long unbroken scroll is grouped into labelled zones (new `ParentSection`/`ParentZoneHeading`, mirrors the child Home pattern): "Your plan" / "Your children" / "Account" wrap their cards; the two self-nulling promo cards carry their own zone heading ("This month" on Mastery, "Needs your approval" on Requests) so no empty headers render. Zone content uses a v4 important-suffix margin-neutraliser (`[&>*]:mt-0!`) so each card's standalone margin doesn't fight the zone rhythm. **(2) Premium billing card** (`SubscriptionCard`) — the active state is rebuilt as a trustworthy card: tonal medallion + "Premium" title + status detail + a tone-matched status badge (Active/Trial/Ending/Payment due), divider, full-width manage action (shared `renderActive` for native+web active branches); inactive/upsell cards moved off `border-2/rounded-lg` onto canonical `rounded-2xl border`. Tested plan-picker contracts (radiogroup/radios/savings/note/button names) unchanged. **(3) Premium-request card** recolored off amber (= reward/streak elsewhere) onto the brand palette under "Needs your approval", solid Approve + outline Decline at 44px. tsc/lint clean; 39 parent/subscription/child tests pass; build green. **Deferred (own future work):** collapsible Account rows; i18n of the (pre-existing English) billing detail strings. **Phase-2 remaining for the programme: Admin dense-form section system** (ModuleForm/ChallengeForm/AdminSettings) — Learn needs no structural Phase-2 work.

- **Figma UI-review programme — Admin/Parent/Learn Phase-1 baseline consistency sweep — ✅ shipped 2026-06-24** (main `4a2f538`/`e616855`/`d8856a9`, CI green `28110188209`, **frontend-only, no migration**; web aliased to `frontend-n58xxr9hl`; `cap sync ios`). After mapping all three remaining surfaces against the Home/Arcade design language, the work split into a **Phase-1 mechanical baseline** (objective token/component/radius fixes, no Figma per the trivial-tweak exemption) and a **Phase-2 Figma structural redesign** (next). Phase-1 shipped, surface order Learn→Admin→Parent: **Learn** — `VideoLesson` (lone outlier) wrapped in the canonical lesson-card shell (`rounded-3xl bg-white p-6 shadow-lg shadow-brand-600/10`), amber/emerald state panels → accent/success tokens, video frame → `rounded-2xl border-line`; `CardLesson` padding `p-7`→`p-6`. **Admin** — `AdminAnalytics` migrated off the hardcoded `gray-*`/`red-*` palette onto tokens (KPI cards → `rounded-2xl … shadow-sm`, brand range-toggle, 44px target); `CollectablesAdmin` raw `rounded border px-2 py-2` fields → token field style + brand focus ring, buttons normalised to `rounded-md`, drops-table row hover; `FeedbackList`/`VideoHealthList` table row hover + 44px pagination. **Parent** (paying-customer trust surface, most drift) — `ChildCard` `rounded-lg`→`rounded-2xl` to match every other parent card; raw tier `<select>` tokened; `MasteryReportCard` hardcoded grays → tokens; `ChildAnalytics` sub-12px text (`text-[10/11/13px]`) lifted to `text-xs`/`text-sm`; `SignInMethods` raw biometric checkbox → shadcn `Switch`+`Label` (test asserts `switch` role). Verified: tsc/lint clean, 27 lesson + 129 admin + 28 parent component tests pass, build green, full suite at env-only baseline. **Phase-2 Figma structural redesign remaining: Parent dashboard information architecture + premium billing (`SubscriptionCard`) + premium-request urgency colour; Admin dense-form section system. Learn needs no structural Phase-2 work.**

- **Figma UI-review programme — Arcade (surface 2) — ✅ shipped 2026-06-24** (main `d738464`, CI green, **frontend-only, no migration**; web aliased; `cap sync ios`). Figma-approved Arcade polish (board `GhzRq1n5ZT6dC6q5VTAhD3`) across all three screens: **hub** plain list rows → vibrant gradient game tiles (orange→red / green) + Play chip + coins chip header; **Quiz Rush** energetic gradient start hero + celebratory results card (big score, best/coin chips, gradient Play again) + chip-style HUD + rounded answer buttons; **MoneyWord** light polish (subtitle, softer rounded tiles + keyboard, one cohesive green completion card). Start/answer/results + role=grid/keyboard/share/legend contracts preserved; gradients deepened for white-text contrast; Arcade hub test wrapped in a QueryClient (now uses `useProgress`). tsc/lint/10 game+hub tests/build green. **Review queue remaining: Admin · Parent.**

- **Figma UI-review programme — Home (surface 1 of 4) — ✅ shipped 2026-06-24** (main `62cf5d2`, CI green, **frontend-only, no migration**; web aliased; `cap sync ios`). First surface of a standing **Figma-first UI review** of the most-churned screens. Home's flat ~11-card pile was regrouped into **labelled zones** (Figma board `VuzqLarSKJ18HGtKIp6Ddc`, approved): merged the market-XP into the `MarketChip` (new optional `xp` prop), tightened `StatsCard` under the hero, added a **Keep learning** group (Revise + a Browse-all card) and a **Play** group (featured drop full-width + MoneyWord/Arcade as a **2-up grid** instead of 3 stacked cards), Shortcuts + slim premium below. Same content/components, clearer hierarchy. Deviation from the mockup: `StatsCard` kept as its own component (carries the daily-goal bar / goal-met / freeze count) rather than dissolved into the hero. tsc/lint/144 child tests/build green; full suite env-only baseline. **Review queue remaining (Figma-first, one at a time): Admin · Parent · Arcade** (`/arcade` hub + `/arcade/quiz-rush` + `/arcade/moneyword`).

- **Avatar & collectables art-direction polish pass** — ✅ shipped to prod 2026-06-24 (main `cf7fcef`, CI green, **NO migration** — frontend art + seed; Vercel web aliased; `cap sync ios`). Translated a **Figma-approved** art direction (file `ZRPXleB54UXJDqoPPBDiCt`) to inline SVG: **Penny** got a gradient body + soft top highlight, glossy catch-light eyes, pink snout + inner-ears and rosy cheeks (same viewBox/props/moods/skins/accessories — works at 28px nav up to the 160px showcase); the **5 scene backgrounds** (beach/forest/city/space/vault) were redrawn richer with gradient skies + layered depth, replacing the flat 3-rect placeholders; and **3 new drop-ready collectable accessories** (halo, winter_beanie, flower_crown) were added to `Penny.tsx` + the slot map and **seeded as `drop_eligible` pool items** (no unlock_type — inert until an admin schedules them on `/admin/collectables`), giving B2 real art to drop. Verified visually (Figma board + `visualize` previews at real sizes) and by gates: FE tsc/lint/40 ui+shop tests/build, BE ruff + 82 cosmetics/collectables/seed (AvatarStage space-fill test + cosmetics-seed count updated for the new gradient/items). **New standing rule (AGENTS.md `453eaa7`): non-trivial UI/visual enhancements go through Figma first.** Deferred (own future specs): app/profile themes; full Penny outfits beyond recolour.

- **Limited-Edition Collectables (B3 — Home featured-drop card)** — ✅ shipped to prod 2026-06-24 (main `a1adf07`, CI green, **NO migration** — frontend-only; Vercel web aliased; `cap sync ios` done). A compact **featured-drop card on the child Home** that spotlights the **soonest-ending, not-yet-earned** live limited drop with emoji + rarity badge + countdown + live progress bar, tapping through to the shop's Limited shelf. Reuses the existing B1 `useCollectables()` hook (no new endpoint/query). Mounted above `ArcadeDailyCard`; **self-hides** when nothing is live / all earned / data undefined (no layout shift). Built via SDD (2 commits + opus whole-branch review = READY TO MERGE, no Critical/Important): a behaviour-preserving **shared extraction** (`collectableBits.tsx` = `rarityClass`/`formatCountdown`/`ProgressBar`, now shared by `LimitedShelf` + the card; `LimitedShelf` tests pass unchanged) then the card itself (`FeaturedDropCard` + a pure cache-safe `pickFeatured`). **This completes the Limited-Edition Collectables programme — B1 (earn-only drops engine) + B2 (admin drop scheduler) + B3 (Home card) are all LIVE.** Spec/plan: `docs/superpowers/{specs,plans}/2026-06-24-collectables-home-card*`.

- **Limited-Edition Collectables (B2 — admin drop scheduler)** — ✅ shipped to prod 2026-06-24 (main `ff3ae8b`, CI green, **migration `d5e6f7a8b9c0` applied to prod** = 1 additive col `drop_eligible BOOL NOT NULL DEFAULT false` on `cosmetic_items`; Vercel web aliased; `cap sync ios` done; ship-now no-snapshot per user — additive with server default). A console page (`/admin/collectables`) to **schedule limited drops over a dev-supplied art pool** instead of seed-only. Model: a drop stays a `CosmeticItem` with the B1 columns set (B1 untouched); `drop_eligible` marks dev-shipped pool art (art lives in `Penny.tsx`, so a dropped accessory never renders blank). Pool item = `drop_eligible AND unlock_type IS NULL` (inert, hidden everywhere — shop filter tightened to `unlock_type IS NULL AND drop_eligible IS false`). Backend: `collectables_admin_service` (list_pool/list_drops with computed `scheduled`/`live`/`ended` status + `owned_count`; schedule/edit/unschedule with guardrails) + admin router `/admin/collectables` (GET pool, GET drops, POST schedule, PATCH edit, POST unschedule), all `get_current_admin`-gated. **Lifecycle guardrails (server-enforced):** scheduled drops fully editable incl. full-replace; **live drops rule-frozen** (only `available_until`, future-only = "end early"); **never hard-delete** — `unschedule` blocked once live (`not_scheduled`) or owned (`owned_cannot_unschedule`), so earned `UserCosmetic` rows are immutable. Frontend: `CollectablesAdmin` page (scheduled-drops list w/ status chip + owned_count + status-driven actions [scheduled→Edit·Unschedule, live→End early, ended→view-only]; schedule/edit form over the pool dropdown). Built via SDD (6 commits, 1 fix wave [distinct `not_scheduled` code + future-only live end-date], opus whole-branch review = READY TO MERGE no Critical/Important; a post-review spec-gap close added the scheduled-Edit affordance). B1↔B2 contract verified by an integration test (a B2-scheduled live drop grants through the unchanged `grant_eligible` seam). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-24-collectables-admin-scheduler*`. **Operator:** devs ship drop-ready cosmetic art → admin schedules drops on `/admin/collectables`. **NEXT (last sub-project): B3 = Home featured-drop card.**

- **Limited-Edition Collectables (B1 — earn-only drops engine)** — ✅ shipped to prod 2026-06-24 (main `22cace2`, CI green, **migration `c4d5e6f7a8b9` applied to prod** = 5 additive **nullable** cols on `cosmetic_items` [`available_from`/`available_until` tz-datetime, `rarity` str(12), `unlock_type` str(20), `unlock_threshold` int]; Vercel web aliased; `cap sync ios` done; ship-now no-snapshot per user — additive nullable). Limited cosmetics with a **timed availability window + rarity label**, **earned from achievements** (not purchasable). Engine: `collectables_service` with an `unlock_type` evaluator registry (`streak_days` / `window_xp` / `window_lessons` / `window_arcade`), `is_drop_active(item, now)` (sync, inclusive null-window bounds), and **idempotent `grant_eligible(session, progress)`** that is **savepoint-isolated** (per-drop `begin_nested()`) and defensive (outer try/except returns `[]` — never raises into the XP flow). Grants fire on the shared **`award_xp` seam** (after `_add_market_xp`) **and** a nightly **`POST /internal/collectables/reconcile`** cron (CSRF-exempt, cron-secret auth). API: `GET /collectables` → `{active:[DropOut w/ live goal progress], owned:[OwnedOut]}`; shop **excludes** drops (`unlock_type IS NULL` filter) and `buy_item` 403s `not_buyable` for them. Frontend: `LimitedShelf` on `/shop` (rarity badge, countdown, live progress bar, earned state, owned-collection equip) + lesson-complete **earn toast** when `granted_collectables` is non-empty. Seed ships ONE example drop (**Founder's Crown** — legendary, `streak_days`/7, 30-day window) so the shelf renders live. Built via SDD (9 commits, opus whole-branch review; **final review caught a Critical** — `granted_collectables` wasn't plumbed through the lesson-complete response so the earn toast was dead on the primary path — fixed `ef36e1b`, which also reordered `record_daily_activity` before `award_xp` so streak drops grant on the completing lesson). **Two post-build/post-push crash fixes:** `35d3b49` guards `LimitedShelf` against undefined/partial `useCollectables()` data; `22cace2` guards avatar-less `LeaderboardTable` rows (`r.avatar?.accessories`) — the latter was an **actual red CI** on the Stats a11y test inherited from the avatar-everywhere commit, now green. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-24-collectables-core*`. **NEXT (decomposed): B2 = admin drop scheduler (schedule/author drops from the console); B3 = Home featured-drop card.**

- **Avatar Everywhere** — ✅ shipped to prod 2026-06-24 (main `2c95fd9`, CI green, **NO migration**; Vercel web aliased; `cap sync ios` done; verified live). The child's Penny avatar (equipped skin + accessories) now shows in the **top-left nav of every page** (replacing the InvestiKid icon) **with their name**, and as a **mini Penny on every leaderboard row** (all scopes). Backend: `leaderboard_service` attaches each row's equipped cosmetics via one batched query → `AvatarOut` on `LeaderboardRowOut`; avatars derive from existing `UserCosmetic`/`CosmeticItem` (backgrounds excluded). Privacy unchanged (avatars are aria-hidden, non-PII; public scopes still consent-gated). Built via SDD (3 commits, opus whole-branch review = READY TO MERGE, no Critical/Important). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-24-avatar-everywhere*`. **NEXT (the user's 2nd ask): limited-edition collectables — own spec (brainstorm pending).**

- **Leaderboard scope & privacy** — ✅ shipped to prod 2026-06-24 (main `bd63c5e`, CI green, **migration `b3c4d5e6f7a8` applied to prod users table** — confirmed in Railway logs; Vercel web aliased; `cap sync ios` done; ship-now no-snapshot per user). One leaderboard at three scopes — **My Market** (default) / **Global** / **Friends** — with an **XP / Arcade-Points** metric toggle, unified in `LeaderboardCard` on Stats (replaces the two old boards). **Kids'-privacy model:** public (market/global) boards show a non-identifying auto-generated **handle** (`<Adj><Animal><NN>`, kid can reroll; zero free text) and include a child **only when `leaderboard_consent AND NOT leaderboard_hidden`** — **parent grants consent (default OFF), child can hide**. Friends boards (closed, parent-made groups) show usernames. Backend: migration + `handles.py` generator + `leaderboard_service.py` (single privacy choke point) + `GET /leaderboard?scope=&metric=` (rate-limited) + `/me/handle` (+reroll) + `/me/leaderboard-visibility` + `POST /parent/children/{id}/leaderboard-consent`; `ChildOut`/`/me/handle` return persisted consent/hidden so toggles reflect saved state. Built via SDD (12 commits, opus whole-branch review = READY TO MERGE, no Critical/Important; privacy invariant traced end-to-end). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-24-leaderboard-scope-privacy*`. **NOTE: a global public board for under-13s warrants a compliance (COPPA / UK Children's Code) review before wide launch — the design supports a central kill (consent defaults false).** Deferred polish (non-blocking, in SDD ledger): a couple of cosmetic items already cleaned; none outstanding that block. **Post-ship UX fixes (2026-06-24, live):** the child **settings/profile dialog** was frozen off-screen on shorter viewports — root cause was the shared `DialogContent` (no max-height + a stuck `animate-in` entrance pinning it above the viewport); fixed with global `max-h-[90dvh] overflow-y-auto` + a top-anchored, animation-disabled override on that dialog (verified in-browser: on-screen, scrollable, interactive). Also made **Penny's Shop** a top-level item in the account dropdown (one click from the avatar) in addition to the Home chip.

Full build track **M3–M9** (home redesign, product analytics, pricing/packaging,
outcome paywall + Mastery Report, daily goal + push foundation, Penny cosmetics,
group challenges + seasonal events) · **SP-Bio Face ID login** · **iOS push parity**
(FCM bridge, verified E2E) · **parent-management dashboard** (access bridge,
back-to-app, full logout) · **web auth** (same-site `api.investikid.ai`) · prod
**Firebase** credential · **CRON_SECRET** rotated · **R2 upload-size** enforcement ·
LLM token instrumentation · device-QA fixes (login cookie race, safe-area,
BottomSheet portal, ChildCard wrap, toasts) · **app icon** finalised.

- **LLM topical guardrails** — ✅ shipped to prod 2026-06-16 (regex input gate + shared preamble across all 9 LLM surfaces + structured guardrail logs + adversarial test suite).
- **Security: `starlette` ≥1.3.1 bump** — ✅ in prod (`starlette==1.3.1` / `fastapi==0.137.1`; clears GHSA-wqp7/x746/jp82/82w8; pip-audit clean).
- **Simulator stock-page crash fix** — ✅ shipped to prod 2026-06-16 (drop NaN price-history rows at the provider + guard `StockChart` against null close).
- **Cron hardening** — ✅ in prod 2026-06-16 (`video-health-cron` per-endpoint isolation + retries + corrected default URL; validated by manual run).
- **Frontend Dependabot — 0 open** — ✅ 2026-06-16 (vite/form-data/@babel/core patched within Vite 6, then **Vite 6→7** major upgrade shipped to prod; the 2 remaining esbuild alerts dismissed as `not_used` — Deno-install-path #17 and Windows-dev-server #22, neither reachable in our Node/Vite/macOS-Linux setup).
- **Revision / spaced-repetition ("Revise")** — ✅ shipped to prod 2026-06-16 (home card + Revise tab/hub + capped-5 weak-first sessions with mastered-concept refreshers; LLM-generated cached/moderated questions; per-correct XP feeds the daily goal/streak; wrong refresher re-enters the SR loop; reuses the existing SR engine — **no migration**; WCAG 2.2 AA, vitest-axe).
- **Market-content operator tooling (markers + replace + batch)** — ✅ shipped to prod 2026-06-20 (main, **no migration**; admin routes 403-unauth = live). Three improvements to the E2/E2.1 admin lesson-generation workflow, found while operating it: (1) **published-lesson badge** per level (from the existing `lesson_count`) so an operator can see which levels are already done; (2) **replace-on-regenerate** — `POST /admin/levels/{id}/approve-drafts {replace}` atomically deletes a level's existing lessons then approves the moderation-safe drafts (one transaction; an **empty safe-draft set never empties a level**), surfaced as "Regenerate (replace)" on Market Content + an "Approve all" / "Publish & replace existing (N)" commit on the review screen — fixes that approving previously *appended* (duplicated) lessons; (3) **batch generation** — `POST /admin/modules/{id}/generate-market {include_populated}` (server-side GB-source resolution, skips populated levels by default, **best-effort per level with rollback** so one level's failure can't leak partial drafts) + a per-module "Generate all levels" button and a market-wide "Generate all" that runs each module sequentially (progress, bounded 429 backoff/retry, include-populated toggle). Backend ruff + 28 tests; frontend tsc+lint+**1070 tests**+build; `cap sync ios`. Also fixed two prod bugs in the same pipeline earlier today: market-generate `KeyError: 'video'` (skip non-generatable lesson types) and an `extract_json_list` helper hardening the suggester + tips against object-wrapped JSON arrays. Spec/plan `docs/superpowers/{specs,plans}/2026-06-20-market-content-markers-batch*`.
- **Seamless onboarding + native child-initiated purchase (Ask-to-Buy)** — ✅ shipped to prod 2026-06-20 (main, **no migration**; Railway + Vercel `app.investikid.ai` 200, `/billing/child/*` 401-unauth = live). Makes the kids' app more Duolingo-like AND lets a child start a subscription themselves on native, mediated by Apple **Ask to Buy** / Google Play **family approval** — instead of the internal email request (kept for **web**). (1) **Onboarding** verify+polish — signup already collects `parent_email` only where the child's country+age requires it (`resolve_policy`); locked with a regression test, consent copy made threshold-agnostic ("the rule where you live") for the EU-16 markets. (2) **Backend** `household_key(user)` (parent_email, or a self-managed teen's own email) + `recompute_household_premium` generalized for the teen self-household (behavior-neutral for normal households); **child-scoped** `/billing/child/*` endpoints (account-token, apple/google verify, plans) authed by `get_current_user`, scoped solely from the authenticated child (no request field can cross households), reusing the existing verify services unchanged. (3) **Lightweight parental gate** (arithmetic, friction-not-auth) before the native purchase sheet. (4) **`runNativePurchase`** shared helper (discriminated-union per-platform deps; pending-aware) — SubscriptionCard refactored onto it. (5) **Child paywall** native path: gate → purchase → `active` (unlock) / **`pending`** ("asked your grown-up" — Ask-to-Buy deferred, NO early unlock; entitlement flips later via webhook + the daily reconcile) / `cancelled`; web keeps the email flow + a native "ask a grown-up" fallback link. Self-managed teens can self-purchase. Backend ruff + 39 billing/consent tests; frontend tsc+lint+**1064 tests**+build; `cap sync ios` (native path rides the next TestFlight build). Spec/plan `docs/superpowers/{specs,plans}/2026-06-20-seamless-onboarding-native-purchase*`. **⚙️ OPERATOR/App-Store before this transacts:** the gate + OS Ask-to-Buy are the consent mechanisms (Ask-to-Buy only mediates on a Family-Sharing **child** Apple ID — otherwise the OS sheet's own Apple-ID auth applies); kids-category review of the parental-gate-before-purchase flow; and the M5 pricing go-live (Stripe/Apple/Google products) is still required for ANY purchase to complete. A legal review of the consent-age matrix + kids-purchase flow is recommended before public launch.
- **Pre-TestFlight hardening** — ✅ shipped to prod 2026-06-20 (main, **no migration**; Railway + Vercel `app.investikid.ai` 200). Three fixes from a pre-TestFlight review: (1) **persistent login** — silent single-flight refresh-on-401 + retry-once in `apiFetch` using the existing 30-day refresh token, so non-biometric users stay logged in for the refresh window instead of being bounced every ~15 min (no backend change; entitlement still checked server-side per request); (2) **subscription freshness** — `recompute_household_premium` now requires an unexpired `current_period_end` (null still entitles), PLUS a daily `subscription_reconcile_service` cron (`/internal/subscriptions/reconcile`, CSRF-exempt, `X-Cron-Secret`) that re-pulls authoritative Stripe/Apple/Google state for at-risk rows so a missed webhook self-heals without falsely revoking auto-renewals; (3) **streak-reminder nudge** — a one-time native `StreakReminderNudge` on Home prompting an engaged kid (live streak) to enable the off-by-default reminder, OS-permission-respecting. Backend ruff+tests, frontend tsc+lint+1048 tests+build, `cap sync ios` done (Units 1 & 3 ride the next native build). A deploy-verification probe caught + fixed a CSRF-allowlist miss on the new cron endpoint (would've left the reconcile unreachable). Spec/plan `docs/superpowers/{specs,plans}/2026-06-20-pre-testflight-hardening*`.
- **Localization + multi-market programme** — ✅ **ENGINEERING COMPLETE, all live in prod** (2026-06-17 → 2026-06-20). Global rollout engine across 10 markets (GB/US/AU/CA/IE/ES/FR/DE/HK/SG) + 6 UI languages (en/es/fr/de/zh-Hant/zh-Hans). Sub-projects, each spec'd/planned/TDD'd/promoted: **0** Gemini model lineup (lite=Flash-Lite, standard=Flash, premium+verifier=gpt-5-mini, Together fallback) · **A** i18n foundation (`users.language`, react-i18next, no-literal-string lint) · **B** AI replies in the user's language (+ multilingual moderation) · **C1** `Market` entity + market-based content gating · **C2a** per-market progress (`UserMarketProgress`, switchable active market, invariant-safe XP) · **C2b** multi-market kids' UI (picker, switcher, per-market home/stats, coming-soon) · **D** cross-market rewards (coins + "Market Mastered" badges) · **E1** content-translation pipeline (stored, moderated, curated-overridable) · **multi-market premium gate** (free users get ONE started market; premium unlocks all) · **E2** per-market content-wave pipeline (verified `MarketBrief` → scaffold from GB → market-grounded generation → review → publish) · **E2.1** intelligent market content (UK-residue adaptation guard on drafts + model-proposed market-specific modules with one-click create + market-native generation). Ships **INERT** for current users (everyone on GB); the gate/new markets only bite once an operator publishes non-GB content. Specs/plans: `docs/superpowers/{specs,plans}/2026-06-1[7-9]-*`. **Operator content production now UNDERWAY — GB/US/HK LIVE (2026-06-22, see next bullet); 6 markets remain** (AU/CA/IE/ES/FR/DE/SG). No localization engineering outstanding.

- **Age ceiling removed (anyone ≥ min_age can use the app)** — ✅ shipped to prod 2026-06-22 (main, no migration). Module browse/access no longer excludes users for being *older* than a module's `max_age`; only the lower `min_age` floor is enforced (`content_service.is_module_age_ok` + `recommendation_service._apply_hard_filters`). Fixes adults/over-16 users seeing a blank learner home. Target audience stays ~10-18; `max_age` is retained on modules for authoring/recommendation but no longer gates visibility. Full suite 1374.
- **Tiered lesson depth + module archive/purge + server-side content pipeline; GB/US/HK content LIVE** — ✅ shipped to prod 2026-06-22 (main, beta straight-to-main; backend migration `d4f6b8c0e2a1`, snapshot waived/declined; api+web 200). A run of admin-content improvements found while operating the market pipeline, plus the first real per-market content: (1) **Tiered lesson depth** — levels now generate **10 / 15 / 20** lessons for complexity tier 1/2/3 (`LESSONS_PER_TIER` / `target_lessons_for_tier`; the curriculum batch passes an explicit `target_count`, the ad-hoc per-level path stays 1-per-concept), built as a **teach-card + practice-quiz pair per concept**; the designer now emits **5/8/10 concepts/level** + **8–10 modules** for parity and uses `max_tokens=16000` so the larger tree doesn't truncate. (2) **Module archive + 30-day purge** — `modules.archived_at` (NULL=active); admin **Delete** soft-archives (refuses a **live/published** module with 409 — unpublish/replace first), **Restore** un-archives, **publish_market_curriculum auto-archives** the modules it retires, a migration **backfills** genuinely-retired modules, and a cron-gated `POST /internal/purge-archived-modules` (CSRF-exempt, added to the daily workflow) hard-deletes >30-day-archived modules (cascade). Admin Modules page gains an **Archived** section (countdown + Restore) **and a market-filter tab + per-row market badge**. (3) **Server-side market-content pipeline** (`market_content_pipeline.py`: scaffold→generate-per-level→publish) behind a cron-gated `POST /internal/market-content?market=&action=` + a `generate-market-content.yml` workflow (`gh workflow run`, `scaffold=false` resumes) — generates a market end-to-end **without the admin UI**. (4) **LLM credit/billing fallback** — the provider chain now advances on a billing/credit/quota error (`_is_provider_unavailable_error`), so the authoring tier (Opus→gpt-5-mini) keeps generating when Opus is out of credits. **CONTENT NOW LIVE** (current counts after the 2026-06-22 diversity regen): **GB** (10 modules/448 lessons; old UK curriculum auto-archived), **US** (9/405, regenerated in American English; old cruft auto-archived), **HK** (9/401, English + HKD-grounded; localize to zh-Hant later via E1) — all `has_content=true`, verified market-native, generated on **gpt-5-mini** (Opus out of credits; ~$5 for the 3-market run vs ~$70 Opus; auto-returns to Opus once topped up). Backend ruff + full suite (1362); frontend tsc+lint+tests+build. Specs/plans: `docs/superpowers/{specs,plans}/2026-06-21-module-archive-purge*` + `2026-06-21-lesson-depth-tiered*`. Also this session: **CI speed-up** (A11y job de-duped + Playwright browsers cached → ~3.7→1.4 min), **beta straight-to-main** flow (skip testing/staging in beta; reinstate at release — see `docs/deployment-environments.md`), and a **prod login fix** (`vite.config` now bakes `VITE_API_BASE_URL` from the build env — Vite only read .env files, so a Vercel-injected base was dropped; restored the missing Production env vars too).

- **US content diversity + American-English regen; prod login same-site fix; investing→simulator missions** — ✅ shipped to prod 2026-06-22 (main, beta straight-to-main, **no migration**; api+web 200). Three threads while reviewing the new GB/US/HK content: (1) **Content diversity + market-correct English** — generation now rotates character names/scenarios + a 3-format card/quiz/scenario mix with a sibling-aware `avoid` list, writes **American English for US/CA** (`_market_english`), and the designer spreads concepts across distinct facets per level (no Roth-IRA/ISA stacking). US re-generated + republished; verified **0 British-term leakage** + varied characters. GB/HK already had the diversity fix and healthy spread (audited live proposals — ISA/MPF/FSCS concentration is market-appropriate, not clustering), left as-is. (2) **Prod web login fix (recurrence)** — the Vercel Production `VITE_API_BASE_URL` had been re-created as the cross-site `investikid.up.railway.app`, making the `SameSite=None` auth cookie third-party (Safari/Chrome block → `/me` 401 → "Could not start your session"); re-set to the **same-site `api.investikid.ai`** + redeployed (per `2026-06-15-web-auth-same-site-api-design.md`); invariant + regression history documented in `docs/deployment-environments.md`. (3) **Investing→simulator missions** — investing modules (growth/compound, risk/diversification) now auto-attach **one "apply in the simulator" mission** to their culminating lesson (`first_buy` or `diversify`-3), 20 XP + admin-configurable per-market virtual cash (`simulator.investing_mission_cash`; default GB/US 100, HK 1000; per-mission cash still editable in the admin LessonForm). `investing_missions.sync_investing_missions` is idempotent (one per live investing module, classified by title, on the final lesson); `publish_market` re-syncs after a republish; `/internal/market-content?action=sync-missions` + `sync-investing-missions.yml` backfill already-published markets (ran for GB/US/HK → **6 missions live**). **Reward-integrity fix:** `evaluate_apply_missions` now scopes to the child's `active_market_code` — the predicates read global portfolio state, so without it one first buy would complete every market's `first_buy` mission and multiply the reward. The `ApplyMissionCTA` renders automatically (no frontend deploy). Backend ruff + full suite (1373).

- **Video lessons curation (salvage + forward suggestion)** — ✅ shipped to prod 2026-06-23 (main, **migration `e7c1a2b3d4f5`** = additive `video_candidates` table, snapshot waived/declined; built via subagent-driven SDD on a `video-lessons` branch, 8 commits, per-task + whole-branch reviewed → READY TO MERGE). Brings back the hand-curated YouTube **video lessons** that the GB/US/HK regen archived (the AI generator only makes card/quiz/scenario and skips `video`). One admin **Video Curation** review queue (`/admin/video-curation`) holds `VideoCandidate` rows from two sources — **recovered** (a one-off, idempotent, topic-matched extraction from archived modules, `POST /internal/video-candidates/extract`, cron-gated + CSRF-exempt + a `workflow_dispatch` workflow — defuses the ~2026-07-22 purge) and **suggested** (`POST /admin/video-candidates/suggest` → YouTube `search.list` `safeSearch=strict` → filtered to embeddable/non-age-restricted). An operator previews each (embedded), picks the destination module+level (editable dropdowns, topic-pre-filled), and **approves → a `video` lesson** — gated so only `embeddable is True` videos land (shared `video_embeddability` helper; output otherwise unchanged). Admin endpoints behind `get_current_admin`. Backend ruff + 1392 pytest; frontend tsc + lint + 1098 vitest (incl. vitest-axe) + build; all green on the merged main. **OPERATOR next:** trigger the extraction workflow once, then review/approve in `/admin/video-curation` BEFORE ~2026-07-22. Deferred minors (final review): suggested-path `health_detail` dead; localize the YouTube query for non-EN markets. Also this push: greened two pre-existing frontend CI reds from earlier today (DeleteAccount.tsx i18n-lint on the intentionally-English legal page; ParentDashboard test querying the moved Back-to-App as a button not a link). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-23-video-lessons-curation*`.
- **Video curation — archived-module guard + cross-market placement** — ✅ in prod 2026-06-23 (main `6fa8ac7`, **no migration**; web deployed + aliased, backend via Railway CI). Operating the new queue surfaced a real bug: the approve module picker offered **archived** modules, so the two recovered GB videos were approved back into their *archived* source modules — live in **zero** markets (archived modules are invisible to children). Fix: (1) `VideoCuration` now filters `useModules()` to live modules only, so both the suggest panel and the per-row approve picker exclude archived modules; (2) `approve_candidate` rejects an archived `module_id` with 422 (defense-in-depth for non-UI/cron callers). Also **re-placed the two videos correctly across all three English markets** (operator decision: "What is a stock?" → each market's *Risk & Diversification* module; "Compound interest" → each market's *Growing/Compound* module — 6 live `video` lessons, GB/US/HK, first level of each, created via the admin API). The stale duplicate copies left inside the archived source modules are invisible and harmless (left in place). Backend ruff + 6 video-curation pytest (incl. new archived-module test); frontend tsc + lint + VideoCuration vitest (incl. new archived-exclusion test).
- **Email links un-clickable — `app_base_url` scheme fix** — ✅ in prod 2026-06-23 (main `b551471`, **no migration**, backend via Railway CI). Root cause: `APP_BASE_URL` was set to a **scheme-less** value (`app.investikid.ai`) in prod, so every transactional email link — **verify-email**, the **parent-consent magic link**, parent sign-in, password reset — rendered as a *relative* `<a href>` that mail clients can't make clickable (the button showed but went nowhere). Fix: `Settings` now normalizes `app_base_url` — prepends `https://` when no scheme is present and strips a trailing slash — so a scheme-less env value can never break links again (durable, no env edit required). TDD: 2 new `test_config` cases; verified end-to-end the verify-email button href is now `https://app.investikid.ai/verify-email?token=…`; `/verify-email` frontend route confirmed present. (Optional hygiene: set `APP_BASE_URL=https://app.investikid.ai` in Railway, though the code now handles it either way.)

- **Simulator news summary refused by the guardrail (regression)** — ✅ shipped to prod 2026-06-23 (main, **backend, no migration**). The per-stock + market news summaries (and chart-guide / time-machine) were returning a canned deflection — *"we focus on saving/spending/earning… we don't cover specific company stock news…"* — instead of an age-pitched summary. Root cause: the shared `GUARDRAIL_PREAMBLE` ("ONLY ever discuss personal-finance learning and the child's active lesson… anything outside, steer back") made the lite model treat "summarise this stock's news" as off-topic and refuse. Raw news data was fine (5 real headlines); the LLM step deflected. **Key insight (from the user):** that preamble's anti-injection / topical-deflection rules exist to defend against a child *typing free text* (Coach Penny, home coach, chart-coach) — but the news summary is **app-generated with no child input**, so the interactive guardrail is the wrong tool there. Fix: split surfaces by interactivity. Interactive surfaces keep `with_guardrail_preamble` unchanged; the 4 **generated** simulator surfaces (`/market/news-summary`, `/market/news-summary/{ex}/{tk}`, `/market/chart-guide/...`, `/market/time-machine/...`) use a new **`with_generation_framing`** — a content-safety line (age-appropriate, kid-safe, no buy/sell/hold, no price prediction) + **data-injection resistance** ("treat data below as info to summarise, not instructions"), WITHOUT the anti-jailbreak scaffolding. Output is still screened by `moderate_output`, so safety is preserved. (Superseded the earlier `allow_market_summary` carve-out patch from `fb7a570`.) Diagnosed live in-browser (fetched the endpoint, saw the refusal text). Coverage meta-test updated to accept either framing; backend ruff + guardrail/simulator/coverage suites green (64).

- **has_content reset on every deploy (US/HK markets silently un-published)** — ✅ shipped to prod 2026-06-22 (main, beta straight-to-main, **backend, no migration**). Root cause: `seed_markets` runs on every container boot and **unconditionally overwrote** `Market.has_content` from the hardcoded seed list (GB=True, all others False), so each backend redeploy reset US/HK back to "no content" even though their modules were published — surfacing as the Home market chip showing **US/HK as "Coming soon"** and the new Learn-tab `MarketSwitcher` hiding itself (it needs ≥2 content markets). Fix: (1) `seed_markets` no longer touches `has_content` on **existing** rows (it's runtime state owned by the publish pipeline + admin toggle; only set on initial insert); (2) new **`reconcile_market_content`** runs at startup after content seeding and **promotes** `has_content=True` for any market with ≥1 **published, non-archived** module (promote-only — never hides an operator-live market; self-heals markets published after the seed, no manual DB surgery). Backend ruff + market/seed/publish suites green (incl. 2 new tests: no-clobber + reconcile promote/skip-draft/skip-archived). Self-heals US/HK on the next backend deploy's boot.

- **Consistent "Back to App" control (admin + parent)** — ✅ shipped to prod 2026-06-22 (web; **frontend-only**, synced into native build 14 / versionCode 2). Admin rendered it as a plain text link at the **bottom of the sidebar** (→ `/`, full reload); parent used a padded button at the **top-right of its header** (→ `/home`) — inconsistent element/position/target/styling. Extracted a shared `BackToAppButton` (`common.backToApp`, `Link → /home`, one styled treatment) used in both, placed **identically**: directly under the brand/title, left-aligned (admin moved up from the sidebar bottom; parent moved from the top-right header group to under the brand). Parent keeps its `hasAppSession` gate — it only renders for bridge users who actually have an app login (standalone magic-link parents get a `/me` 401 → no data → hidden). Verified live in-browser on both. tsc + lint + admin/parent suites green.

- **Shell resilience + navigation polish (ErrorBoundary + scroll-to-top on route change)** — ✅ shipped to prod 2026-06-22 (web; **frontend-only**, also synced into native build 14 / versionCode 2). Two launch-quality fixes in the child Shell, both found during testing: (1) **Route-level ErrorBoundary** wrapping the routed `<Outlet />` — there was previously **no error boundary anywhere**, so any page render error unmounted the whole React tree → blank screen with no nav ("no way to get back"); now a page crash shows a recoverable `role=alert` fallback while TopNav + BottomTabBar stay mounted (the crash is still logged for diagnosis), and the per-route remount auto-clears it on tab change. (2) **Route scroll/focus** — `useRouteFocus` called `main.focus({ preventScroll: false })` on every client nav, which scrolled the focused `<main>` into view; on pages **taller than the viewport** (Stats/Progress/Simulator) this aligned the content top to the screen top and pushed the header (logo + tabs) off-screen, while **short** pages (Home/Learn) were unaffected — hence the inconsistency the tester saw. Now scroll to top on route change + focus with `preventScroll: true` (keeps the a11y focus move, drops the scroll jump). Verified live in-browser on Stats + Progress (header visible at top). Tests: boundary renders fallback-not-blank + axe-clean; route-focus scrolls-to-top + preventScroll + skips first render. tsc + lint + build clean.

- **Learn-tab market switcher (real switch, in place)** — ✅ shipped to prod 2026-06-22 (main, beta straight-to-main, **frontend-only, no migration**). Fixes "selecting a different market in the Learn tab doesn't switch markets." The Learn tab rendered the legacy `RegionSwitcher`, which sets `content_region` — an axis that **no longer gates lesson visibility** (child modules filter solely by `active_market_code`), so flag taps invalidated `['modules']` but the refetch came back unchanged. Replaced it (Learn tab only) with a new **`MarketSwitcher`** that drives the *real* market via `useSwitchMarket` → `POST /me/active-market`, so lessons re-fetch for the new market in place without leaving the page; it offers only `has_content` markets (hides empty ones), highlights the active one, and routes a **locked** (premium-gated, free-user) market to the paywall (`PremiumRequestKind` gains `'market'`; backend `kind` is free-form, no change). `RegionSwitcher` stays in the **profile menu** for the legacy region/exchange preference (simulator featured exchange + parent analytics). New component has unit + vitest-axe coverage (6 tests); frontend tsc+lint+build clean. The canonical Home `MarketChip → /markets` picker was already correct and is untouched. (NB: switching market here does not also sync `content_region`, so the simulator's *featured exchange* still follows the profile-menu region — minor, pre-existing; flag if it confuses testers.)

- **Penny's Arcade Phase 2 (MoneyWord)** — ✅ shipped to prod 2026-06-23 (main, beta straight-to-main, **migration `a2b3c4d5e6f7`** = additive `arcade_words` table + `arcade_daily_play` table, snapshot waived/declined). Daily Wordle-style finance-vocabulary puzzle: one shared word per language per UTC day, 6 guesses, variable 4–8-letter words, per-letter color feedback (correct/present/absent) with correct duplicate-letter handling, emoji share grid. Server-authoritative (answer never sent until complete; guesses server-tracked + daily-unique constraint); once-per-day reward via Phase-1 coins/Arcade Points + streak. Word bank is LLM-assisted + moderated + admin-approved — ~18 English words seeded at launch so the daily puzzle works immediately; **operator must run Suggest → approve on `/admin/arcade-words` to grow the bank**. New routes: `/arcade/moneyword` (child game), `/admin/arcade-words` (admin word-bank review — status filter, Suggest N button, per-row inline-editable approve/reject). Home daily card (`ArcadeDailyCard`) shows Play/Continue/Done state. English-only at launch (`language="en"` throughout). Backend ruff clean + 1451 pytest passed; targeted arcade/moneyword vitest (24 tests across 6 files, incl. vitest-axe) all pass; tsc + lint (0 errors) + build clean; `cap sync ios` done. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-23-pennys-arcade-phase2-moneyword*`.
- **Avatar & Penny Cosmetics (avatar showcase + backgrounds + skins)** — ✅ shipped to prod 2026-06-24 (main, **no migration** — reused existing `CosmeticItem.type` column + `equipped` bool). Grew `/shop` into "Penny's Shop & Avatar": a layered avatar stage (scene background → skin body → accessory) rendered as inline-SVG, category tabs (Accessories / Backgrounds / Skins). **Equip model (updated 2026-06-24, main `9c70a98` + slots `819a04b`, no migration, stacking verified live):** accessories **stack across slots** (hat + eyewear + bow together) but are **mutually exclusive within a slot** — one hat at a time (slot map in `cosmetics.py`: head = the 4 hats + headphones, eyes = sunglasses + monocle, bow = own slot). Backgrounds + skins stay single-pick (swap on equip). Unequip is per-item (`POST /cosmetics/{id}/unequip`); `useEquippedCosmetics()` returns `accessories: string[]` and Penny renders every equipped accessory glyph. Also fixed `db10276`: `ArcadeDailyCard` guards malformed MoneyWord data (`data?.guesses?.length ?? 0`) — was a flaky unhandled-async crash turning CI red. **2026-06-24 follow-ups (`9947679`/`e038be8`/`22ba88d`, verified live):** (1) MoneyWord **Share** now uses the native share sheet + clipboard fallback + a visible "Copied!" confirmation (was a silent clipboard write that looked broken); (2) **Penny's Shop is now discoverable** — a persistent "Penny's Shop" chip (🐷 + coin balance) on the Home quick-links row links to `/shop` (was only in the account menu); (3) `formatCurrency` now tolerates null/undefined/NaN (was crashing the Simulator portfolio hero → flaky CI). Full vitest suite now has ZERO unhandled errors. **Avatar render bug fixed (`ed710d8`, verified live in-browser):** equipped accessory emoji were invisible — the `<text>` glyphs inherited `fill="none"` from the Penny `<svg fill="none">` root, so hats/glasses/bow were in the DOM but never painted (the face showed because it sets its own fill). Each accessory glyph now sets an explicit `fill`; regression test added. **Party Hat redrawn (`721d9d2`, verified live):** was 🥳 (a whole partying *face*) which looked unprofessional on Penny's head; now a hand-drawn flat-SVG striped cone + pom-pom via a new `ACCESSORY_SVG` render path. **ALL accessories now flat-SVG (`3d317a0`, verified live + `cap sync ios`):** the remaining seven (sunglasses, bow, headphones, grad_cap, crown, monocle, top_hat) redrawn as hand-drawn flat SVG in the scene-background style; emoji `ACCESSORY` map + `<text>` path removed entirely; each renders in a `data-accessory` group. Shop catalog tile for party_hat also moved 🥳→🎉 (seed). Native iOS project re-synced so the next Xcode/TestFlight build bundles this whole session's web work (accessory stacking/slots, avatar render fix, all-SVG accessories, Arcade share fix, Home shop chip, currency guard). Two new cosmetic categories seeded into the catalog: **scene backgrounds** (beach/forest/city/space/vault) and **body skins** (pink/sky/mint/gold/lavender, free/premium mix). Equipped skin + accessory now propagate everywhere Penny appears (PennyFAB, Coach slide-over). Coins + premium gate reused as-is. Note: app-wide theme + profile avatar customisation (background colour scheme etc.) are a separate, future spec. Backend ruff clean; 1 pre-existing pytest date-tz failure (unrelated); frontend tsc + lint (0 errors) + 24 targeted cosmetics vitest + build all clean; PennyFAB test updated to add `QueryClientProvider` wrapper (the FAB now calls `useEquippedCosmetics`); `cap sync ios` done. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-??-avatar-cosmetics-*`.

- **Penny's Arcade Phase 1 (games hub + Quiz Rush)** — ✅ shipped to prod 2026-06-23 (main, beta straight-to-main, **migration `f1a2b3c4d5e6`** = additive `arcade_scores` table + `UserProgress.arcade_xp_today`/`arcade_xp_date` columns, down_revision `e7c1a2b3d4f5`, snapshot waived/declined). A dedicated games hub that lives alongside the lesson track, giving kids a break-style activity with dual rewards. Components: (1) **Home `ArcadeHomeCard`** — daily-return entry point on the Home screen, routes to `/arcade`. (2) **`/arcade` hub page** — lists available games (Quiz Rush; MoneyShop/MoneyWord are Phase 2). (3) **`/arcade/quiz-rush` Quiz Rush** — 60-second timed quiz blitz reusing the existing quiz bank, with unlocked-concept preference and a cold-start fallback (< 10 unlocked → draw from global pool). (4) **Backend** `arcade_scores` table + `arcade_service` (weekly per-market leaderboard, personal bests, capped **25 coins/XP per day** via the `award_xp` seam) + `quiz_rush_service` (server-authoritative session/scoring) + `/arcade` router (session, score submission, leaderboard). **Dual rewards:** capped coins/XP (arcade daily cap, separate from lesson cap) + uncapped Arcade Points. Both games are free (no premium gate). Backend ruff + full pytest suite (1408); arcade-specific vitest (7 tests, incl. vitest-axe) pass; tsc + lint + build green; `cap sync ios` done (new `/arcade` screens ship in the next native build). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-23-pennys-arcade-phase1*`. **Phase 2 (MoneyShop / MoneyWord daily word game + daily-schedule engine)** is the next separate plan.

---

## 🔴 Now — launch-critical path (mostly human)

| Item | Owner | Notes |
|---|---|---|
| **iOS 1.0 (16) → TestFlight** | ✅ 2026-06-25 | Archived in Xcode + uploaded. Bundles the whole programme (UI-review redesigns, Penny's Shop, MoneyWord daily-reset fix) + native biometric/push. Next: add the processed build to a TestFlight group + assign testers. |
| **Android versionCode 3 (1.0) → Play** | ✅ 2026-06-25 | **First Android release** — signed AAB uploaded to **Internal testing** (signed with the upload key; Play App Signing enrolled). ⚠️ Kids-app store gates still required before wider tracks/production: Families / target-audience, Data safety, content rating, privacy-policy URL. |
| **Recruit + distribute beta cohort (M2)** | 👤 | 10–20 families across **8–10 / 11–13 / 15–18** bands (the teen band feeds M10). Comp premium generously. Share the TestFlight + Play Internal opt-in links. |

## 🟠 Next — operator config to transact & submit (no code)

| Item | Owner | Notes |
|---|---|---|
| **M5 pricing go-live** | ⚙️ | Stripe monthly+annual Prices (GBP/HKD); App Store Connect `premium_annual` + price points + **Apple Small Business Program** (15% vs 30%); Play Console `premium_annual`; set `*_ANNUAL` env vars. *Until done, IAP can't transact (the "something went wrong" on Subscribe).* |
| **M11 App Store Connect listing** | ⚙️ | Paste copy/keywords from the [listing kit](launch/2026-06-15-app-store-listing-kit.md); capture 5 screenshots; enter **privacy nutrition labels**; confirm **age rating** (4+ vs 9+/12+ — 🤔 decision in the kit). |
| **Confirm OpenAI account spend cap + billing** | ⚙️🤔 | 30-sec check so premium (`gpt-4o-mini`) doesn't silently fall back to Llama. No top-up needed for beta; size the real cap post-beta from token data. |

## 🟡 Soon — code I can do when scheduled

| Item | Owner | Notes |
|---|---|---|
| **M10 teen-testing prep** | 💻 | Figma visual-direction mockups (darker/flatter theme, dialled-back mascot, denser simulator) for the teen sessions — *before* any Investor-Mode code change. Then one iteration pass from findings. Needs the beta cohort recruited first. |
| **Native → `api.investikid.ai`** | 💻 | Migrate `NATIVE_API_FALLBACK` to the same-site API in a future native build (cleaner; build 6 keeps the railway URL, which still works). Low priority. |
| **Testing/staging web same-site** | 💻⚙️ | Replicate the `api-testing` / `api-staging` subdomains if web QA on those envs is needed. Deferred (prod-only fix shipped). |

## 🔵 Later — launch readiness (M12) & teen validation (M10)

| Item | Owner | Notes |
|---|---|---|
| **M10 · Teen validation** | 👤💻 | 5–8 real 15–18s, structured script → documented findings + one shipped Investor-Mode iteration. Runs once beta cohort exists. |
| **M12 · Launch readiness** | 👤💻⚙️ | Full device-QA sign-off on the launch build (billing, video, auth, progress-save, offline on real HW); observability/alerts check; support-email + feedback-triage flow; Railway load sanity; **prod backup cadence** confirmed. |

## ⚪ Post-launch bets (sketched, not committed)

- **School/teacher packaging** (highest-margin hedge) — start with 2–3 teacher chats during beta, zero code.
- **Android / Play Store launch** — billing built; needs device-QA matrix + listing.
- **Regional curriculum depth** — the **engine is built and live** (localization + multi-market programme, incl. the E2/E2.1 per-market content pipeline). What remains is **operator content production**: per empty market run brief → verify → (suggest/scaffold) → generate → expert-review → publish, then enable content languages via E1. This is a content/ops effort on shipped tooling, not new code.
- **AI Coach proactivity** — weekly personalised "your plan this week," premium-gated.
- **R2 self-hosted video** — parked; YouTube covers lessons. Enable Cloudflare R2 (5 env vars) only if/when self-hosted video is wanted; upload-size enforcement already in place.

---

## How items graduate

Non-trivial work runs the superpowers pipeline: **brainstorm → spec
(`docs/superpowers/specs/`) → plan (`docs/superpowers/plans/`) → TDD on `testing` →
gated promotion** (`testing → staging → main`; prod DB-snapshot ask before any
migration). Operational items (M1/M2/M5/M10/M11/M12) are runbooks/checklists with
little-to-no code. Mark items off here as they ship.

**After every push to `main`, update the progress docs as part of the same task** —
move the shipped item into "Live in prod" here, and refresh `docs/superpowers/PROGRESS.md`
and the relevant roadmap entry. Shipping isn't done until the trackers reflect it.
