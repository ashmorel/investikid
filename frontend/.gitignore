node_modules
dist
dist-ssr
*.local
.env
.env.local
.DS_Store
playwright-report
test-results
*.tsbuildinfo
vite.config.d.ts
vite.config.js

# Generated pseudo-locale (i18n extraction proof; never committed)
src/locales/en-XA/

# Capacitor iOS build artifacts
ios/App/App/public/
ios/App/Pods/
# SPM lockfile — regenerated by `npx cap sync ios` + Xcode (the iOS CI job
# resolves packages fresh each run), and Xcode rewrites it constantly, which
# otherwise dirties the tree and blocks pulls across Claude/Codex/Xcode.
ios/App/App.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved
